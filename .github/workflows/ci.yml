name: CI Pipeline - Student GPA Calculator

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-lint:
    runs-on: ubuntu-latest

    steps:
      # Stage 1: Source & Environment Setup
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      # Stage 2: Linting
      - name: Run Checkstyle (Linting)
        run: mvn checkstyle:check

      # Stage 3: Build (Resolve dependencies)
      - name: Build with Maven
        run: mvn clean package -DskipTests

      # Stage 4: SCA (OWASP Dependency-Check)
      - name: OWASP Dependency-Check
        uses: dependency-check/Dependency-Check_Action@v3.0.5
        with:
          project: 'Student-GPA-Calculator'
          path: '.'
          format: 'HTML'
          out: 'dependency-check-report'

      - name: Upload OWASP Report
        uses: actions/upload-artifact@v4
        with:
          name: owasp-dependency-check-report
          path: dependency-check-report

      # Stage 5: SAST (CodeQL)
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: java

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3